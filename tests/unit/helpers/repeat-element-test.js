import { repeatElement } from 'sync-web/helpers/repeat-element';
import { module, test } from 'qunit';

module('Unit | Helper | repeat element');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = repeatElement(42);
  assert.ok(result);
});
