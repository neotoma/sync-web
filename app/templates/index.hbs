{{#if sessionsService.hasJobsToStart}}
  {{create-jobs-section}}
{{else if sessionsService.jobs}}
  {{dashboard-section}}
{{else if cart.declinedNotificationRequests}}
  {{notice-section header="Sorry we couldn’t be more helpful!" body="If you'd like, you can view the options again to see if there are others that might interest you." buttonLabel="View options" buttonRouteAction="viewStoragesPicker"}}
{{else}}
  {{intro-section}}
{{/if}}
