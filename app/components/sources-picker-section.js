import PickerSectionComponent from './picker-section';

export default PickerSectionComponent.extend({
  header: 'Where do you want to copy data from?',
  layoutName: 'components/picker-section',
  modelName: 'source',
  submitRoute: 'cart'
});